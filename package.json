{
  "name": "your-root-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start --prefix server\" \"npm run start --prefix client\"",
    "deploy": "npm run build --prefix client && node server/index.js"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
